# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Env
.env
.env.local
.env.*.local

# Claude Code (per-user, not shared) — any system
**/.claude/settings.local.json

# Build
node_modules/
dist/
dist-ssr/
elm-stuff/

# Cadence/gateway: secrets, SQLite, Go build output (never tracked — live on the fly volume)
tokens.json
tokens.db
*.db
cadence-server

# Vite local env files (any system)
*.local
