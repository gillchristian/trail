# Backlog

Ordered. The top *unchecked* item is next. Promote into `CURRENT.md` when started.

Conventions:
- Completed tasks stay here, checked `[x]` with their PR number — the Active section doubles as a one-line shipping index. The full record lives in `DONE.md`.
- TASK ids are global and monotonically increasing across all of `planning/`: next id = highest id appearing anywhere (Active, parking lot, Proposals, DONE) + 1.

## Active

- [x] TASK-001 — Scaffold the Elm + Vite + Tailwind app, lift Gpx.elm from crest, basic "hello / upload / parse" working end-to-end — (M) ✓ PR #1
- [x] TASK-002 — IndexedDB storage layer + race index page (upload GPX → race appears, persists across reload) — (M) ✓ PR #2
- [x] TASK-004 — True-1:1 profile view (port crest's Main.elm rendering into the Race page) — (M) — ✓ PR #3
- [x] TASK-005 — Aid-station CRUD: add by distance-from-start or distance-from-previous, edit, delete, persist — (M) — ✓ PR #4
- [x] TASK-006 — Per-km planning view (left card with 1:1 mini-profile, right inputs: notes + pace, prev/next nav) — (L) — ✓ PR #5 (combined with TASK-007)
- [x] TASK-007 — Pace distribution engine (Tobler, ADR-0003) + total-target UI, locked vs auto kms — (M) — ✓ PR #5
- [x] TASK-008 — Planning table view (km / section toggle) + CSV export in both modes — (M) — table done in PR #5; CSV export ✓ PR #6
- [x] TASK-009 — GPX export with aid-station waypoints (ADR-0002), downloadable from the race page — (S) — ✓ PR #6
- [x] TASK-010 — `.trail` project file export/import (round-trip: GPX + plan + aid stations) — (M) — ✓ PR #6
- [x] TASK-011 — Gamified visual pass: UTMB-DNA badges, layered/ghost-wave elevation, race-card aesthetic, glow accents, micro-animations — (L) — ✓ PR #8
- [x] TASK-012 — Offline-first: PWA manifest + service worker for app shell + IDB confirmed durable — (M) — ✓ PR #7
- [x] TASK-013 — Real-world map overview view via Leaflet/OSM JS port — (M) — ✓ PR #10
- [x] TASK-003 — Race metadata editing (name inline + date/location/url/notes + cover image upload) — (S) — ✓ PR #9
- [x] TASK-025 — Fix pace bug: per-km Target Time should be clock time (moving + aid rest in that km); Pace stays moving-only. Flows through km card, km table, section table, section card. Manual-typed target subtracts in-km aid rest before storing. See `samples/aid-station.png` for the screenshot the user flagged. — (S) — ✓ PR #34
- [x] TASK-026 — Show HR avg per km on linked actuals (per-km card + km table). Extend the streams fetch to include `heartrate` if it isn't already; persist on `ActualSplits.splits`. — (S) — ✓ PR #35
- [x] TASK-027 — Skeleton/pulse loading state on the home page drop component while a GPX parse is in progress. Pulse animation on the whole drop area, "Parsing <filename>…", input disabled during parse. Applies to file-drop, file-select, and Strava picker paths. — (S) — ✓ PR #36
- [x] TASK-028 — Home page two-section split: "Plans" (no `actualSplits`) and "Executions" (linked). Find better naming if "Plans / Executions" doesn't feel right; the cut is linked-actual vs. not. Section headers + empty states. — (S) — ✓ PR #37
- [x] TASK-029 — Chunked SVG profile path so the elevation chart renders end-to-end on long tracks (Cocodona 250 stops mid-track at 10 m/px). Browser SVG single-path rendering limit; not in Elm. — (S) — ✓ PR #41
- [x] TASK-030 — Populate plan-table Pace / Time / Current-sum from `Predictor.predict` at intensity = 1.0 when `race.plan.targetSeconds = Nothing`, so a freshly-uploaded race shows a sensible plan immediately. Display-only; commit happens on first slider interaction. — (S) — ✓ PR #42
- [x] TASK-036 — Sync ship-status across the planning + reference docs: the predictor/Strava track shipped, but four docs still frame it as pending (2026-06-10 doc-vs-code audit). `reference/pace-prediction-roadmap.md:3` header still reads "proposal — exploration only, nothing committed" though TASK-014..021 + 024/024b all merged (DONE.md:26-35); its §0/§2 (lines 12, 75, 86) describe the engine as distributor-only when `Predictor.predict` (`src/Predictor.elm:53`) + the profile model ship, and line 296 *promised* a brief update on TASK-024 landing that never happened. `reference/cadence-backend-spec.md:3` says "not yet greenlit" though `src/StravaApi.elm` consumes the live API and its §9 "what trail has to build" list is fully built; `…-addendum-1:64,70` still treats TASK-024 as unshipped. `BACKLOG.md`'s own Proposals section (lines 47-54) lists TASK-014..021 as unpromoted with no `[x]`/PR, unlike the TASK-023/024 strike-through convention. `whiteboard/training-as-analysis.md:91` frames TASK-026 (HR on actuals, shipped PR #35) as "Queued." Flip the status headers to "implemented", strike-through + PR-annotate the shipped proposals, leaving only TASK-022 (calibration) genuinely open. Docs-only. — (M) — ✓ PR #65
- [x] TASK-037 — Rewrite `reference/project-brief.md` + `reference/glossary.md` to match shipped reality (2026-06-10 audit). Brief: "No backend, ever" (lines 13, 28) is contradicted by the live Strava/cadence integration — adopt the roadmap's agreed wording (`pace-prediction-roadmap.md:296`: Layer-0 offline / Layer-1 opt-in Strava sync); add the three undocumented shipped features (plan-vs-actual splits + HR via `ActualGpx`; athlete profile + `Predictor` at `#/profile`; aid-station CSV import/export via `AidCsv`); fix feature 10 (map shipped as the `<trail-map>` custom element, deliberately *not* a JS port); patch the minor drifts (CSV accepts miles vs "km only" line 31; `Browser.application` not `.element` line 38; IDB port is ~100 lines / 2 stores, not ~50 / 1, line 41). Glossary: **VMH is defined wrong** (line 29 says flat-ground km/h; code uses `verticalRateVmh` = vertical metres/hour of climb — `src/AthleteProfile.elm:47`, `src/Predictor.elm:114` — the flat rate is the separate `flatTrailPaceSecPerKm`); add the user-visible terms it omits (distance category S/M/L/XL, elevation density, flat-equivalent distance — all shown on the race cards). Docs-only. — (M) — ✓ PR #67
- [x] TASK-038 — Fix technical-accuracy drift in the ADRs + local-CI + MORNING docs (2026-06-10 audit). ADR-0003: the slope-factor properties table (lines 26-28) lists *un-normalized* values — f(+0.10)≈1.419 not 1.687, and the curve is symmetric about −5 % not 0 (`src/Planning.elm:327`); two documented interactions don't exist ("Reset plan" — only per-km `ResetKmToAuto` exists; all-Manual "target becomes derived" — the committed target is kept, `src/Main.elm:6511`); note the independent-rounding caveat (auto sum can drift a few s) + that the slope divisor is the window length, not a fixed 1000 m. ADR-0002: `sym` is auto-derived from services (`src/GpxExport.elm:112`), there's no per-station UI picker and the no-services default is "Flag, Blue" not Restaurant; the `<desc>` example + "Aid 1/Aid 2" unnamed-fallback don't match the exporter. `cadence-backend-spec.md`: the streams example (lines 154-156) omits the `{"data":[…]}` nesting the real backend returns (`src/StravaStreams.elm:107`) — a client built from it can't decode; the env var is `VITE_BACKEND_URL` not `BACKEND_URL` (line 257); the addendum's "fields flow through to trail's settings" (line 119) is false (no `/api/athlete` client) and its "§14.2" citation points at the roadmap when the HR-zone model lives in `archive/trail_race_planner_spec.md`. `local-ci.md`: document the global Elm 0.19.1 prerequisite (it's not an npm dep, and `smoke-aid-csv.mjs` uses `npx --no-install elm`, so `npm install` alone can't run gates 1/2/4) + qualify the storage-smoke claim (line 14 covers only the v1 `races` store, not the v2 `settings` store/upgrade path). MORNING.md: dev port is 5174 (`package.json:7`) not 5173 (line 45); the parking lot is mid-file, not "at the bottom" (line 90); the "JS-storage-smoke-only / needs nvm 22 later" caveat (line 92) predates both the `.nvmrc` v22 pin and the Elm aid-CSV harness — refresh or archive the file. Docs-only. — (M) — ✓ PR #69
- [x] TASK-039 — Fix the section-overlap double-count in `Planning.sectionsForRace`. A km window straddling an aid-station distance `b` matches the overlap test (`km.distStart < b && km.distEnd > a`) for *both* adjacent sections, so its index lands in two `kmIndices` lists and `sumKmField` adds its whole gain/loss twice — inflating section-table Time/gain/loss/cum (`Main.elm:5060`/`5071`/`5109-5110`), the section card's stats + `containedKms` (`5234`/`5236`), the actual-vs-plan Δ (`sectionActualSeconds`, `6332`), and section-mode CSV (`Csv.elm:188`). Assign each km to exactly one section (midpoint containment); pro-rating considered. The `sumKmField` comment already *claims* center-based assignment — the fix makes the code match it. Promoted from parking lot 2026-06-15 (scoped during TASK-025). Fixed by midpoint partition + a new `smoke:sections` gate; see ADR-0004. — (M) — ✓ PR #72
- [x] TASK-040 — Separate `gpxText` into its own IDB row so plan-only saves (slider commit, aid-station edits) don't re-ship the ~3 MB GPX string across the FFI. PR #29 worked around it by deferring the save off the drag hot path; this is the schema refactor that makes those saves small. Promoted from parking lot 2026-06-15. Shipped as a `gpx` IDB store (DB v2→v3 + migration), light vs full save paths; see ADR-0005. — (M) — ✓ PR #74
- [x] TASK-041 — Fix the `Planning.elm` `slopeFactor` docstring (lines ~322-324): it repeats the un-normalized values TASK-038 corrected in ADR-0003 — "10 % uphill ≈ 1.69×" is really 1.419×, and "20 % grade either way ≈ 2.40×" is wrong twice (the normalized curve is symmetric about s = −0.05 not 0; f(−0.20)=1.419, f(+0.20)=2.01). One-comment fix; touches `src/` (why it was held out of docs-only TASK-038). Promoted from parking lot 2026-06-15. — (S) — ✓ PR #76
- [x] TASK-042 — Print-friendly export of the planning table: a print stylesheet / print path so the km (and section) plan table prints cleanly on paper — legible, paginated, dark-theme chrome stripped. Promoted from parking lot 2026-06-15. Shipped as a Print button (`Dom.print` port) + `@media print` stylesheet. — (S/M) — ✓ PR #78
- [ ] TASK-022 — Calibration from past activities (**parent epic — not itself a shippable unit**). Split 2026-06-15 (roadmap §7, by value-per-effort) into per-fit sub-tasks: **TASK-043** (climb-rate `vmh`, ✓ shipped PR #80) + **TASK-044** (flat-trail-pace, ✓ shipped PR #82) — the two core continuous rates done — with climb-fatigue `k` / Riegel / sustainable-HR-by-duration / descent-technique / decoupling left in roadmap §7 as future sub-tasks (more complex / data-gated / some need new predictor capabilities; promote with user appetite). Promoted from Proposals 2026-06-15 (explicit user go-ahead). — (L, split)
- [x] TASK-043 — Calibrate climb rate (`vmh`) from linked actual runs: pure `Calibration.fitVmh` (gain-weighted realized climb rate over climb kms across linked runs) + a transparent "calibrate from your runs" panel on `#/profile` that updates `verticalRateVmh`. First + highest-value split of TASK-022. Shipped with `smoke:calibration` + ADR-0006. — (M) — ✓ PR #80
- [x] TASK-044 — Calibrate flat-trail-pace from runnable-km actuals (same data path as TASK-043). Shipped as `Calibration.fitFlatPace` + a second row in the calibrate panel; ADR-0007. — (M) — ✓ PR #82
- [x] TASK-045 — Section plan time is clock time (moving + the section's aid rest) so the section Δ-vs-plan compares clock-vs-clock — the section lift of TASK-025 (which deferred it), unblocked by TASK-039's partition. New `Planning.sectionAidRest` (attribute each aid's rest by its containing km, not `followedByAid`); section table/card Time+Cum+Δ and section-mode CSV all clock; aid rows become non-additive dividers; `smoke:sections` extended; ADR-0008. Promoted from parking lot 2026-06-15. — (S/M) — ✓ PR #86

### Coach collaboration via `.trail` merge (epic, 2026-06-15)

Async, file-based, single-document collaboration: share a `.trail` with a coach, get notes, merge them back — no backend, offline, accountless (Layer 0). Decisions in **ADR-0009** (three-way merge, not CRDT; *freeze the course, merge the plan*); full spec + rationale + acceptance criteria in **`reference/coach-collab-spec.md`**; the 5 open questions (Q1 gates WI-1; Q2–Q5 gate WI-3) in spec §7 — resolve with the user at each gate. Sequenced per spec §6. **The spec's "distance-keyed aids" / "regenerate-only id" premises were corrected against the code on 2026-06-15 — see ADR-0009's *Grounding* section and the spec's *Reality corrections* callout before implementing WI-1 / the aid-id sub-task.**

- [x] TASK-046 — **Brief nuance: file-based collaboration is in scope (Layer 0).** Edit `reference/project-brief.md` *Out of scope* the way the "No backend, ever" line was softened for Strava: async, file-based, single-document collaboration (export → annotate → merge) is now in scope as a Layer-0 feature; server-side multi-user, accounts, and hosted documents stay out of scope. Acceptance: the two *Out of scope* lines ("No social / sharing features", "No multi-user") are nuanced (not deleted), pointing at ADR-0009. Docs-only. — (S) — spec §0 — ✓ PR #89
- [x] TASK-047 — **WI-1: `.trail` format v2 — identity + integrity guard.** A stable `raceId` (share id minted once at race creation, *not* the regenerated IDB key — see ADR-0009 grounding #1) + a `courseHash` (canonical decoded-track hash, computed at GPX import), both embedded in every `.trail`. Import guard: `raceId` ≠ target → block ("different race"); `courseHash` ≠ target → block/warn per **Q1**. Widen the strict version gate (`ProjectFile.elm`) to accept {v1, v2}; v1 files still import via the legacy whole-snapshot path and mint identity on the way in. Acceptance: wrong-`raceId` file blocked w/ clear message + no state change; wrong-`courseHash` blocked/warned per Q1; matching file proceeds to merge; existing v1 `.trail` still imports. **Gated on Q1** (resolve before implementing). Foundation — no deps. — (M) — spec §2 — **Q1 resolved (canonical hash, hard-block); ADR-0010; field named `shareId`** — ✓ PR #91
- [x] TASK-048 — **WI-2: course-freeze invariant.** Codify *freeze the course, merge the plan*: the merge layer never reads/writes track points (course excluded from the merge surface); a re-uploaded course → different `courseHash` → WI-1's guard rejects prior shared files. Light — trail has no course-point editor, so this is mostly stating + enforcing the invariant. Acceptance: merge output is byte-identical to the local course on track points; course re-upload changes `courseHash` and prior shared files are rejected with the explanatory message. Deps: TASK-047 (`courseHash`). — (S) — spec §3 — **enforced structurally via `Merge.withPlanningLayer` + `smoke:merge`** — ✓ PR #93
- [x] TASK-049 — **Sub-task: fork-collision-safe aid-station ids.** Aid stations *already* have stable ids (`AidStation.id = "a" ++ aidStationSeq`), so this is **not** "add ids" — it's making them safe to merge across forks: today the shared per-race counter mints identical ids (`a5`, `a6`, …) on both copies, so two independently-added aids collide. Make new aid ids globally unique (e.g. author/device-prefixed, or a UUID) so add/remove/move are unambiguously diffable across a fork. Back-compat: existing `"aN"` ids keep working (decode unchanged). Acceptance: two diverged copies that each add an aid produce distinct ids; a round-tripped file preserves ids; v1/older races with `"aN"` ids still load. Deps: none hard; feeds WI-3. — (S/M) — spec §4 + ADR-0009 grounding #2 — **device-tagged `Merge.mintAidId` + a `deviceId` (also WI-3/WI-4's author id)** — ✓ PR #95
- [x] TASK-050 — **WI-3 (part 1): three-way merge engine (pure).** Fold a coach's `.trail` into the local plan against a common ancestor: disjoint edits both land, same-field collisions surface as typed `Conflict { base, mine, theirs }` (no `merge failed` runtime path). Ancestor delivery + version scheme per **Q2/Q4**; merge surface = race metadata + aid stations (keyed by fork-safe id) + per-km plan, three-way per field; profile + actual splits owner-authoritative/excluded per **Q3**; conflict-resolution UX per **Q5**. Write per-type merge concretely (no generic merge framework — three reuses before extraction). Emit WI-4's structured diffs as a byproduct. Acceptance: coach per-km notes + owner aid edits → 0 conflicts, both present; same per-km note edited both sides → 1 conflict, human resolves, applied; deterministic; track points untouched; fast-forward import applies with no conflict UI. **Gated on Q2–Q5** (resolved with user 2026-06-15). Deps: TASK-047, TASK-048, TASK-049. — (L) — spec §4 — **WI-3 split for verifiability: this = the pure engine (`mergePlanningLayer`/`classifyVersions`/`resolve`, ADR-0011, `smoke:merge`); integration + UI = TASK-052** — ✓ PR #97
- [x] ~~TASK-052 — WI-3 (part 2): merge integration + review UI.~~ — **DROPPED 2026-06-16 (user): folded into TASK-056**, which now carries WI-3's whole last mile (integration *and* the review UI) as one task that ships last. Its full integration scope — persist `mergeBase`+`version`, `.trail` carries `{base,current,version}`, version-bump on commit, the import→merge entry point + classify, apply the resolved result + emit `Merged` — moved verbatim into TASK-056. Not implemented; no PR.
- [x] TASK-053 — **Backfill `.trail` identity for pre-existing races (at export).** Bug (user, 2026-06-16): races created before WI-1 export as v2 with empty `shareId`/`courseHash` (decoder defaults; nothing backfilled IDB races) → would break the coach round-trip. Pure `TrailSync.ensureIdentity` fills courseHash (from GPX) + shareId (seeded from the stable IDB `id`) when empty; `ExportProjectFile` stamps + persists (light save). At export not on load (hashing needs a parse — lazy for the shared race). — (S) — ✓ PR #100
- [x] TASK-051 — **WI-4: change history — structured log + feed view.** A derived, cosmetic "who changed what, over time" feed (NOT the merge substrate — delete it and merge still works). Typed `ChangeDescriptor`s (AidAdded/Removed/Moved/Renamed/Retimed, KmNote*, KmPace*, RaceRenamed/DateChanged, CourseUploaded, Merged) grouped into `{ entryId, author, timestamp, source, changes }` entries; rendered as a GitHub/Linear-style activity feed in its own tab/modal/drawer (not inline). Appended on local mutation + on merge (merge entries consume WI-3's diffs — no parallel diff engine). Persisted on the race, included in `.trail`; logs union by `entryId` (immutable, conflict-free). Acceptance: feed in its own surface; ordered change-sets with author + timestamp and per-type styled rows; importing the coach's file unions entries with no dupes; deleting the whole history doesn't affect a later merge. Deps: consumes TASK-050's diffs; author-identity shared with WI-3; otherwise independently buildable. — (M) — spec §5 — **`Changelog` diff/union + `Race.history` + `commitRaceEdit` logging + gamified drawer; `Merged` entries await TASK-056; `smoke:changelog`** — ✓ PR #102

### Identity & merge-review UI (companion spec, 2026-06-16)

Companion to the coach-collab epic — full spec + rationale + acceptance criteria in **`reference/merge-ui-identity-spec.md`** (**read its *Reality corrections* callout first** — it grounds the spec against the code). Two strands that fell out of designing WI-3's review step: **WI-5** (identity & authorship — label changes by *person not role*, accountless, ids are claims) and **WI-3·UI** (the suggestion-review surface). **Sequencing (user, 2026-06-16): TASK-054 (WI-5 identity) → TASK-055 (home view) → TASK-056 (merge integration + review UI, ships LAST).** WI-5 is the foundation — TASK-056 and the WI-4 feed's author labels depend on it. **TASK-052 was dropped and folded into TASK-056** (the merge UI ships after the rest is ready). **Open questions: Q-I1–Q-I3 gate WI-5, Q-U1–Q-U5 gate WI-3·UI** (spec "Shared open questions" — resolve with the user at each gate, as Q1–Q5 were for the engine). **ADR pending:** promote the §1.2 identity decisions + §2.2 UI reframe to an ADR (extend ADR-0009 or a new ADR-0012) when WI-5 is picked up — *not* done at ticketing, per the user's "just the backlog" scope. **Grounding correction (in the spec's Reality corrections):** a device-global `deviceId` already serves as the author id (changelog author + version-vector key + aid-id prefix + `entryId`); WI-5 adds a *person*-level `userId` **over** it — `userId` for human identity, `deviceId` stays the device-scoped collision key. **The same person on two devices shares one `userId`** (claimed via adopt-on-import) — that cross-device identity is the problem WI-5 solves, so `userId` is device-independent *by design* and is never derived from `deviceId`. Don't remove `deviceId`; don't re-key `entryId` by `userId`.

- [x] TASK-054 — **WI-5: identity & authorship (foundation).** ✓ PR #116 (slice 4; slices 1–3 #109/#110/#112, boot fix #114), merged `a3509ef`. A person is a locally-minted `userId` (UUID v4, no server); the display name is a mutable label resolved through a directory. One device-global `me = { userId, displayName }` record, **kept separate from `AthleteProfile.Profile`** (the *performance* profile at `#/profile` — a real name collision, spec §1.2 + Reality corrections). Races persist `owner : userId` (never a denormalized name). A local **directory** `{ [userId]: { displayName, nameUpdatedAt } }` resolves all name display. The `.trail` **denormalizes** the `(userId, displayName, nameUpdatedAt)` pairs it references so an importer can show names for people not yet in its directory. **Mint `userId` at exactly two points (§1.4):** first export with no identity yet, and import-as-someone-else with no identity yet — **never on the "yourself" path**, which always *adopts* the file's owner id (the device-link). **Name LWW:** on import, update a directory entry only when the incoming `nameUpdatedAt` is newer (importing an *older* file never reverts a name). **Grounding:** layer `userId` over the existing `deviceId` — `userId` = human identity (`owner`, author labels, directory); `deviceId` stays the device-scoped collision key (version vector, aid-id prefix, changelog `entryId`). Ids are claims, not credentials (no signing). Back-compat: pre-identity `.trail`/IDB decode via `D.oneOf` defaults; a race with no `owner` adopts `me` on first touch. Acceptance (spec §1.6): stable `userId` minted at exactly the two points, never on "yourself"; `me` distinct from the performance profile; races persist `owner` as `userId` (names only in the directory + denormalized in files); rename = one directory row, no per-race migration; import where `file.owner.userId == me.userId` does **not** prompt; import where owner ≠ me prompts yourself/someone-else (yourself adopts; someone-else with no identity chains a name prompt + mints); older-file import never reverts a name; back-compat decodes. **Gated on Q-I1** (dual-id linking — explicit "link this device" action vs punt v1), **Q-I2** (identity store location — localStorage beside `trail.deviceId` vs a small IDB store), **Q-I3** (derived role badge vs names-only) — resolve with the user first. **ADR pending** (promote §1.2). Deps: none upstream; depended on by TASK-055, TASK-056, and the author labels in TASK-056/051. — (M/L) — spec Part 1.
- [x] TASK-055 — **Home view: personal vs others' + filter by person.** ✓ PR #118, merged `01f7a4f` (owner-based grouping; solo path unchanged). Falls out of WI-5 (§1.5). Distinguish **personal vs someone-else's** by `race.owner == me.userId` — **use owner, not last-editor** (a race you own that your coach edited still reads as yours). Filter / group by person on `owner`, labelled from the directory. Emergent property: on a coach's device every athlete plan reads as "someone else's," filterable by athlete and cleanly separated from any races of the coach's own. Acceptance: home view splits personal vs others' by `owner`; supports filtering/grouping by person via directory names; a coach-edited owned race still reads as personal. Deps: TASK-054. — (S/M) — spec §1.5.
- [x] TASK-056 — **WI-3 (part 2): merge integration + suggestion-review UI.** ✓ PR #123, merged `4287ee6` (slice 1: #121 `f330d68`); ADR-0013; Q-U1–Q-U5 resolved. **Completes the coach-collaboration arc.** WI-3's whole last mile — wire the TASK-050 engine into the app *and* build the human review surface. **Absorbs the dropped TASK-052. Ships last** (after TASK-054 identity + TASK-055 home are ready — user, 2026-06-16). **Integration:** (a) persist `mergeBase : PlanningLayer` (last-synced ancestor) + `version : VersionVector` on `Race`; `.trail` carries `{ base, current, version }` (Q2, additive — no format bump, `D.oneOf` defaults). (b) bump `version[deviceId]` on each local plan/aid/metadata commit; set/advance `mergeBase` at share/merge points. (c) **entry point:** importing a `.trail` whose `shareId` matches an existing local race (courseHash matches per WI-1) routes to merge instead of import-as-new; classify (FastForward → apply directly, no UI; Diverged → review; Behind → no-op; Same → noop); handle the dup-shareId self-reimport edge. **Apply path:** `Merge.resolve` fold → `withPlanningLayer` → save → bump version → emit `Merged` into the WI-4 feed. **Design reference:** an exploration prototype lives at `reference/merge-review-prototype.html` — take the **UX/layout** (card list, two equal options, ring+check selection, reassurance row, progress + Apply/Keep footer, confirm-on-dismiss, identity tint not red/green); do **not** copy its icons or markup verbatim, and note its `"Coach" / "Coach's suggestions"` **role labels are exactly the seat-relative bug WI-5 fixes** — use person names ("You" / `<name>`). **Review surface (§2.2):** **Reframe + vocabulary:** present it as reviewing *suggestions* — avoid "conflict / merge / diff / theirs"; use "changes / suggestions / your version / their version"; **person-named throughout** (WI-5): "You" vs `<name>`, with an optional quiet role badge (`<name> · coach`) only if Q-I3 keeps it. **Show only the true-collision residue** — the version vector skipped fast-forward and the engine auto-merged every disjoint edit — plus a one-line reassurance ("M other changes from `<name>` were added automatically"). **Primitive: a card list** — one card per overlap: a context label (what + where, e.g. "Target pace · km 14") and **two equal, person-named options** (You / `<name>`), **no red/green** (distinguish sides by identity — name + initial + a subtle per-person tint; selection = 2px ring + check). **Forced per-card choice** (nothing pre-selected); **Apply enables only once every card is resolved.** **Two explicit exits:** *Apply changes* (applies the chosen value per overlap → bumps version → emits WI-4 `Merged` entries → merge completes) and *Keep my version* (rejects the **entire** import, no state change); the close (X) is a synonym for "Keep my version" — confirm before discarding only if picks already exist (Q-U4). **Forward-compat:** each card carries its km/location so the later course-anchored renderer (v2, Q-U5) is a new renderer, not a rework. Mobile-first, single column (never side-by-side). Acceptance (spec §2.6): only true overlaps shown + auto-merged count surfaced; two equal person-named options with no pre-selection and no red/green, identity-distinguished, ring+check selection; Apply disabled until all resolved, then applies chosen values + bumps version + writes WI-4 entries; "Keep my version" and close both reject the whole import with no state change, confirm only when picks exist; labels resolve via the WI-5 directory (nothing hardcoded "coach"/"athlete"); each card carries km/location; single-column/mobile-safe. **Gated on Q-U1** (modal vs wide drawer), **Q-U2** (forced choice vs pre-select "you"), **Q-U3** (same-field text overlap: pick-one vs hand-merge textarea — carried from engine Q5), **Q-U4** (confirm-on-dismiss copy), **Q-U5** (confirm course-anchored = explicit v2) — resolve with the user first. Deps: TASK-050 (engine, done), TASK-054 (identity — person-named labels / role badge). — (L) — spec Part 2 + §4 / ADR-0011.

### Internationalization (i18n) — English + Spanish (epic, 2026-06-18)

Hand-rolled, **type-driven** localization (English + Spanish), **no i18n library** —
exhaustive translation functions over a `Language` custom type; the compiler
enforces completeness. Full spec + work items in **`reference/i18n-spec.md`**
(**read its *Resolved decisions* callout first** — it grounds the spec against the
user's greenlight steer); architecture in **ADR-0014**; Spanish term choices (the
single cross-PR source of truth) in **`reference/i18n-glossary.md`**. **Scope:
language only.** Units (metric/imperial) were **descoped by the user** to a
separate, unpromoted item — **TASK-070** in the parking lot — with seams left
(`D.oneOf` per-field decoders, a `Context` record) so it's additive later. Default
language derives from `navigator.language`; a footer toggle (`English / Español`,
no flags, right side) switches it; stored in the `settings` IDB store; never
written to `.trail`. **Suggested order: machinery (058→060) before the translation
sweep (061→069);** the translation task boundaries (062–068) are by UI surface and
may flex as the work lands. Promoted from the parking-lot "Multi-language UI" line
on an explicit user steer (2026-06-18).

- [x] TASK-058 — **WI-1: `Language` type + codec.** Self-contained `Language.elm` (`English | Spanish`), ISO-code codec (`toCode`/`fromCode`/`encode`/`decoder`), strict + total leaf decoder (fails on unknown code). A `smoke:i18n` harness covers round-trip + failing-decode; exhaustiveness is the compiler's job (no `_ ->` in `toCode`). Foundation — no deps. — (S) — spec WI-1. — ✓ PR #128
- [x] TASK-059 — **WI-2: `Settings` + persistence + footer toggle.** `Settings.elm` (`{ language }`, `defaultSettings`, `settingsDecoder` with `D.oneOf` per-field default, `encodeSettings`); persist a `deviceSettings` record in the existing `settings` IDB store via a new `saveSettings` port; flags gain `settings : D.Value` + `browserLanguage`; first-run default from `navigator.language`; JS boot awaits the settings read before `Elm.Main.init` (no flash). Wire the **footer language toggle** (`English / Español`, right side, no flags) → `ChangeLanguage` → `saveSettings`; set `<html lang>`. Verifiable: switch persists across reload; `es-AR` first run → Spanish; a v1 settings blob with no `language` → `English`. Deps: 058. — (M) — spec WI-2. — ✓ PR #130 (await-before-init via async IIFE — TLA not in the build target)
- [x] TASK-060 — **WI-3 + WI-5 (language half): `Context` + `Format`.** `Context { language }` + `toContext` threaded as the first arg to localized views (leaf views stop reading `model.settings`); `Format.elm` localizes decimal quantities (Spanish comma) by wrapping the existing rounding — **no new dep, no unit conversion**. Migrate the *display* call sites (km distances + decimal floats) to `Format`; leave CSV/GPX/`.trail` formatters untouched. First visible proof: toggling flips `42.2 km` ↔ `42,2 km`; pace/clock stay `:`. Deps: 058, 059. — (M) — spec WI-3 + WI-5. — ✓ PR #132 (race card + detail migrated; broader decimal sweep folded into the per-surface tasks since `formatFloat` is shared with SVG/inputs/exports)
- [x] TASK-061 — **WI-4: `Translations` module + global chrome.** `Translations.elm` (function-per-key, total over `Language`, typed interpolation, one/other `plural` helper) + translate the header/nav, footer, route titles. Establishes the pattern the rest reuse. Deps: 060. — (M) — spec WI-4. — ✓ PR #134 (shared action labels deferred to where first used — three-uses rule)
- [x] TASK-062 — **Translate: home / upload / race cards.** Index hero + race-count plural, upload banner + states, empty state, density / distance-category / aid-count labels (+ plurals). Deps: 061. — (M). — ✓ PR #136
- [x] TASK-063 — **Translate: race detail + edit dialog + map view.** Detail CTAs, the metadata form (labels + placeholders), cover-image controls, map-view chrome. Deps: 061. — (S/M). — ✓ PR #138 (also the loading/404 status views)
- [x] TASK-064 — **Translate: aid stations + service labels.** Aid CRUD section, CSV import preview/status (+ plurals/warnings), and `Types.serviceLabel` (Types.elm). Deps: 061. — (M). — ✓ PR #140 (display `serviceLabel` localized; exports keep `serviceToString`/English; `formatRest` deferred to 065)
- [x] TASK-065 — **Translate: planning table + per-km + per-section.** Table/section headers, hints, grade-class + effort-tier + predictor labels, the km/section breadcrumbs and interpolated indices, the actual-vs-plan column. Deps: 061. — (L). — ✓ PR #142 (also localized `formatRest`; `section.label` + Strava picker deferred to 069/066)
- [x] TASK-066 — **Translate: profile/settings + calibration + Strava + elevation toolbar.** The `#/profile` performance-profile fields + help text, the calibrate panel, the Strava connect card, the identity card, and `Profile.elm` / `AthleteProfile.elm` labels. Deps: 061. — (M). — ✓ PR #144 (decoupled the skill `<select>` value/display; AthleteProfile labels stay English-as-key; also the Strava picker from 065)
- [x] TASK-067 — **Translate: activity feed / changelog.** `describeChange` typed descriptors (interpolated names/indices, `Merged N changes` plural) + relative-time ("hace …"). Deps: 061. — (M). — ✓ PR #146 (participle log-style ES; engine untouched)
- [x] TASK-068 — **Translate: modals + toasts.** Delete-confirm, identity flows (name / ownership / link), merge-review surface (person-named, counts + plurals), storage-error toast. Highest interpolation/plural density; the merge-review person-named copy aligns with WI-5. Deps: 061. — (M/L). — ✓ PR #148 (`conflict.label` → 069)
- [x] TASK-069 — **i18n QA sweep + exhaustiveness check + `<html lang>`.** grep for stray hardcoded UI strings missed by 061–068; run the spec's DoD exhaustiveness check (temporarily add a third `Language` constructor → confirm the compiler enumerates every untranslated site → revert, documented in the PR); confirm `<html lang>` tracks the toggle; final read-through in both languages. Closes the epic. Deps: 061–068. — (S/M). — ✓ PR #150 (304 sites flagged; also localized `conflict.label`; `section.label` → TASK-071). **i18n epic COMPLETE.**

### Monorepo migration (epic, 2026-06-24)

Transform trail into a monorepo housing five systems (`trail`, `cadence`,
`gateway`, `track`, `reflect`) under `systems/`, fold in the external **cadence**
repo, and stand up a two-tier federated knowledge base (shared root tier +
per-system instances) so one agent per system can work in parallel. Full
contract — locked decisions, target structure, per-PR operations, acceptance
criteria — in **`reference/monorepo-migration-spec.md`**. Five squash-merged PRs,
strictly ordered 0→1→2→4 (PR 3 may land any time after PR 1); the tree builds
green after every PR; exactly one sanctioned non-squash merge (cadence history
import, PR 2). **Namespace note:** these use the `MONO-` id namespace and `mono/`
branch prefix (repo-structural / shared-tier work, spec §3) — *not* the global
`TASK-` counter; per-system work after the migration gets its own namespace
(`TRAIL-`/`CAD-`/`GW-`/`TRACK-`/`REFLECT-`). Two steps need the user (manual
`fly deploy`; re-point cadence's Vercel git connection) — prepared and surfaced,
never done autonomously.

- [x] MONO-000 — **PR 0: framework v2→v3 — path indirection.** The framework
  resolves instance-area locations by *role* from the manifest's Locations block
  instead of hardcoded `../` siblings. Pure framework change (no file moves, no
  new systems); trail keeps working because its manifest declares the same
  locations it already had. De-risks PRs 1–3, which all instantiate v3 instances.
  Spec MONO-000. — (M) — ✓ PR #152, merged `f2b0bd9`
- [x] MONO-001 — **PR 1: restructure trail in place.** `git mv` trail's code into
  `systems/trail/`; split knowledge into the shared tier (root `framework/` + root
  manifest + `reference/specs/`) and a trail v3 instance under
  `systems/trail/knowledge/`; split the manifest into root + system; root
  `CLAUDE.md` becomes dispatch. Trail still builds/deploys from the new path
  (Vercel Root Directory → `systems/trail`). Preconditions: MONO-000. Spec
  MONO-001. — (L) — ✓ PR #154, merged `9c24ab5`
- [ ] MONO-002 — **PR 2: import cadence (bootstrap exception).** Fold cadence
  `client/`→`systems/cadence/` and `server/`→`systems/gateway/` (flattened); one
  sanctioned `--allow-unrelated-histories` merge (recorded in the root manifest);
  fix `fly.toml`/Dockerfile paths for the flattened gateway and verify the image
  builds locally; route cadence's v1 knowledge into gateway+cadence v3 instances
  (unified history → gateway under a tombstone). `tokens.db` does not migrate.
  **Asks the user** to run the live `fly deploy` and re-point cadence's Vercel git
  connection. Preconditions: MONO-001. Spec MONO-002. — (L)
- [ ] MONO-003 — **PR 3: scaffold track + reflect stubs.** Two empty v3 knowledge
  instances (knowledge only, no code; `framework/` not duplicated — pointed at the
  root copy via Locations). Track brief carries the designed MVP work-item
  sequence + `.trace`/`.trail` contract pointers; reflect brief records
  scope-not-yet-defined + an Unknowns list. May land any time after MONO-001. Spec
  MONO-003. — (S/M)
- [ ] MONO-004 — **PR 4: workspace + parallelism wiring.** No root workspace (each
  system self-contained, Locked decision 13). Root manifest documents the
  worktree-per-agent flow, the branch-prefix/id-namespace table, cross-system
  status as a read-time projection, and the shared-tier-edits-only-via-`MONO-`
  discipline. CI path-filtered per system. Preconditions: MONO-001 & MONO-002.
  Spec MONO-004. — (M)

## Parking lot

- ~~**Section-overlap bug.** `Planning.sectionsForRace` double-counts a km that straddles an aid distance (placed in *both* adjacent sections).~~ **→ shipped as TASK-039 (2026-06-15, ✓ PR #72; ADR-0004).** ~~Still parked: the section-card **Δ vs plan** has a moving-vs-clock apples-to-oranges bug at the section level; it needed a correct section partition underneath it, which TASK-039 now provides.~~ **→ shipped as TASK-045 (2026-06-15, ✓ PR #86; ADR-0008)** — section Time/Cum/Δ + section-mode CSV are now clock time (moving + the section's aid rest), so the section Δ-vs-plan is clock-vs-clock.
- ~~**Separate `gpxText` into its own IDB row** so plan-only saves don't re-ship the ~3 MB GPX string across the FFI.~~ **→ shipped as TASK-040 (2026-06-15, ✓ PR #74; ADR-0005).**
- Strava GAP-style descent aggressiveness slider (if Tobler feels off in practice).
- Per-km gain/loss separately for slope-factor (instead of net Δele).
- ~~**Fix the `Planning.elm:323` slope-factor code comment** — repeats ADR-0003's un-normalized values (f(+0.10) is 1.419 not 1.69; symmetric about −5 % not "either way").~~ **→ shipped as TASK-041 (2026-06-15, ✓ PR #76).**
- ~~Race-organiser bulk-import (paste a list of aid stations with distances).~~ **Shipped 2026-06-05 as TASK-031** — CSV *file* import/export (+ cutoff, warm food, notes). Paste-a-table can reuse `AidCsv.parse` as-is when wanted.
- ~~Print-friendly export of the planning table.~~ **→ shipped as TASK-042 (2026-06-15, ✓ PR #78).**
- Light / dark mode toggle (dark default).
- ~~Multi-language UI ("tramos" → "sections" toggle).~~ **→ promoted 2026-06-18 as the i18n epic (TASK-058–069; ADR-0014) — English + Spanish, type-driven, no library.**
- **TASK-071 — Localize `section.label` (the i18n residue).** The only app string the i18n epic (TASK-058–069) left English: `Planning.sectionsForRace` bakes "Start"/"Finish" into `section.label` ("Start → Aid 1"), shown on the section table + section card. It's **CSV-canonical** (`Csv.elm:207` writes it to the section-mode export, which must stay English), so localizing the *display* needs a display/canonical split — either expose the section's start/end as structured fields (`Maybe String`, `Nothing` = terminus) for the view to format with `Language`, or thread `Language` into `sectionsForRace` and call it with `English` from `Csv.elm`/`SectionsHarness` and with the UI language from `Main` (two render paths). Aid names in the label are user content (untranslated regardless). Small, isolated; pick up on a steer. `conflict.label` was the sibling case and was solved in TASK-069 (view re-derives from the typed `conflict.key`); a similar typed-key approach may fit here. — (S/M).
- **TASK-070 — Unit selection (metric/imperial) in Profile/Settings.** Descoped from the i18n epic by the user (2026-06-18): a user *preference*, not a derived value built reflexively. Store SI (already true), convert at the display boundary, a toggle on `#/profile`. Extends `Settings`/`Context`/`Format` along the seams ADR-0014 leaves (per-field `D.oneOf`, the `Context` record) — `units` is purely additive. Back-compat: a settings blob without `units` → `Metric`. The spec's WI-2 `UnitSystem` + WI-5 conversion math (`reference/i18n-spec.md`) are the blueprint. Not promoted — pick up on a fresh steer. — (M).
- Comparing planned vs actual after the race (post-MVP, would need an .fit upload).

## Proposals (not yet promoted — see `knowledge/reference/pace-prediction-roadmap.md`)

The roadmap doc covers the why and the trade-offs. These entries are the *chunks* that came out of it. Most shipped (struck through with their PR below); **TASK-022** (calibration) was promoted to the Active queue on 2026-06-15 with the user's explicit go-ahead (it had been gated on exactly that).

- ~~**TASK-014 — Course summary card additions.**~~ **Shipped.** Equivalent flat distance + elevation density label on race index/overview. PR #16.
- ~~**TASK-015 — Per-km segment classification by grade.**~~ **Shipped.** Colour tag in the planning table derived from `Km.slope`. PR #18, merged `b451e1e`.
- ~~**TASK-016 — Planned-vs-actual upload (manual `.gpx`).**~~ **Shipped.** Snap to course, compute actual splits at planned km boundaries, render diff column. `ActualGpx.elm`. PR #19.
- ~~**TASK-017 — Profile data model + IDB store + settings page.**~~ **Shipped.** Hand-set fields, preset picker (Beginner / Mid-pack / Strong mid-pack / Sub-elite), one global active profile. `AthleteProfile.elm`, `#/profile`. PR #20.
- ~~**TASK-018 — `Predictor.predict` module (Layer B).**~~ **Shipped.** Pure function; climb + descent + runnable + aid; takes profile + intensity. `Predictor.elm`. PR #21.
- ~~**TASK-019 — Bidirectional aggressiveness slider on the planning page.**~~ **Shipped.** Intensity ↔ target time, inverse via bisection of the predictor. PR #22.
- ~~**TASK-020 — Confidence indicator surfacing on predictor output.**~~ **Shipped.** Annotated by profile source. PR #23.
- ~~**TASK-021 — Strava streams parser.**~~ **Shipped.** Reconstructs an `ActualGpx.ActualTrack` from cadence's keyed-object streams response (spec §4.4). Pure Elm decoder + transform. `StravaStreams.elm`. PR #24.
- **TASK-022 — Calibration from past activities.** Fit `vmh`, fatigue slope, optional HR bands. Surface "what changed and why." Depends on TASK-021 (streams parser) + TASK-024 (auth wiring), both shipped. (L — split when picked up.) **→ promoted 2026-06-15 (explicit user go-ahead); split into TASK-043 (climb-rate `vmh`, the first/highest-value fit) + TASK-044 (flat-trail-pace), with the remaining fits tracked in roadmap §7. See the Active queue.**
- ~~**TASK-023 — Decision: OAuth helper or stay manual.**~~ **Resolved 2026-05-15.** Decision: extend `cadence`'s backend. Spec at `knowledge/reference/cadence-backend-spec.md`; cadence shipped all 5 PRs. Addendum 1 (broaden OAuth scope to `profile:read_all` for `max_heartrate` / `weight` / `ftp` / `measurement_preference`) drafted at `knowledge/reference/cadence-backend-spec-addendum-1-profile-scope.md` — pending hand-off to a cadence session.
- ~~**TASK-024 — Strava OAuth integration in trail.**~~ **Shipped.** v1 (PR #25): token storage + Connect/Disconnect. b (PR #26): activity picker + streams fetch + persist via `StravaStreams.parse` → `ActualGpx.computeSplits`.

Open questions to resolve before promoting any of the above: see roadmap §9.
