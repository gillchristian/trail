# Backlog

Ordered. The top *unchecked* item is next. Promote into `CURRENT.md` when started.

Conventions:
- Completed tasks stay here, checked `[x]` with their PR number.
- `REFLECT-` ids, monotonically increasing, starting at REFLECT-001.

## Active

_(none — **stub, scope undefined**. Per MONO-003 + the framework's SETUP ("if the user hasn't
described the product yet, record what is known + an Unknowns list, log a blocker, and **do not invent
a backlog**"), this is intentionally empty. Resolve BLOCKER-001 (scope) with the user, then seed the
first `REFLECT-001` item.)_

## Parking lot

_(none)_
