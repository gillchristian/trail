# CLAUDE.md — reflect (system entry · STUB)

**reflect** is the **Reflect** system in the product arc Plan (**trail**) → Execute (**track**) →
Reflect (**reflect**): post-hoc analysis/learning from executed runs. **This is a knowledge-only stub
and its scope is not yet defined** — see the brief + the open blocker. No code.

**Read these, in order, before doing anything else:**
1. the repo-root manifest `/knowledge/README.md` (repo-wide rules reflect inherits),
2. this system's manifest `systems/reflect/knowledge/README.md` (reflect-local rules + **Locations**),
3. `knowledge/framework/README.md` at the repo root (the shared working system),
4. the `pr` profile in `knowledge/framework/delivery.md`.

## Non-negotiables (reflect)

1. **One task at a time.** Pull from `systems/reflect/knowledge/planning/CURRENT.md`; if empty, promote the top of `BACKLOG.md`. Acceptance criteria first.
2. **Delivery: pr** (inherited ceiling). Branches → PRs you merge yourself, squash-only; `master` sacred. Branch prefix `reflect/`; ids `REFLECT-NNN`.
3. **User-only attribution.** No `Co-Authored-By: Claude …` trailer, no "🤖 Generated with Claude Code" footer.
4. **Verify before declaring done** — once scope + code exist, from `systems/reflect/`.
5. **Journal everything** in `systems/reflect/knowledge/progress/journal.md`.
6. **When stuck, follow `knowledge/framework/when-stuck.md`.** Log blockers to `systems/reflect/knowledge/progress/blockers.md`, then pivot.

## Status: STUB — scope undefined

Unlike track (MVP designed), reflect's scope is genuinely open. The brief
(`reference/project-brief.md`) records what's known + an **Unknowns list** and **does not invent a
backlog**. **BLOCKER-001 is open**: scope definition is the gating input from the user — resolve it
before any backlog or code.

## Quick map (reflect)

- `systems/reflect/knowledge/README.md` — system manifest (Locations, `reflect/`, `REFLECT-`).
- `systems/reflect/knowledge/reference/project-brief.md` — what's known + the Unknowns list.
- `systems/reflect/knowledge/progress/blockers.md` — the open scope blocker.
- `knowledge/framework/` (repo root) — shared working system (changes = `MONO-` tasks).
- Repo-wide rules + system index: the root manifest (`/knowledge/README.md`).
