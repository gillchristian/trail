# Backlog

Ordered. The top *unchecked* item is next. Promote into `CURRENT.md` when started.

Conventions:
- Completed tasks stay here, checked `[x]` with their PR number.
- `TRACK-` ids, monotonically increasing, starting at TRACK-001.

## Active

_(none yet — **stub**. The designed MVP work-item sequence lives in `reference/project-brief.md`
(Skeleton → domain/persistence → library/race-config → CSV import → ⏸ tracking-view design → tracking
view → post-race view). Promote item 1 (Skeleton) as `TRACK-001` when track is picked up — don't
expand the whole sequence into tasks pre-emptively.)_

## Parking lot

- Deferred (post-MVP, from the brief): `.trace` export, `.trail` ingestion, Live Activity.
