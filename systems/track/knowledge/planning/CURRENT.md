# Current task

> One task at a time. When this file is empty, pull the next item from `BACKLOG.md`.

## Active

### TRACK-001 — WI-1: project skeleton
**Source:** BACKLOG (epic "Tracker MVP"; spec `reference/mvp-plan.md` §7 WI-1, §6.1)
**Branch:** track/track-001-project-skeleton
**Acceptance criteria:**
- [ ] app launches in the Simulator (build from `systems/track/Track/`; screenshot)
- [ ] shows an empty Races list (empty-state placeholder; `NavigationStack` root)
- [ ] a stub race persists to disk and survives relaunch (race-bundle dir under Documents/Races/<id>/race.json)
- [ ] iOS deployment target pinned per ADR-0001 (17.4 → 17.0, all targets)
**Notes:**
- Scope = the *shell* only. The full §4 domain model + append-only `events.log`/fsync/projections are WI-2; WI-1 uses a minimal stub `Race` (id/name/createdAt) + atomic `race.json` write. Status badge, duration, create-race form, and detail navigation are later WIs.
- `+` wires to a *stub* race (placeholder for WI-4's create flow) so persistence-across-relaunch is demonstrable.
- ADR-0001: minimise pbxproj edits → WI-1 types live in the existing `ContentView.swift`; deployment-target pin is the only build-setting change. Promoted the gitignored autogenerated scheme to a committed **shared** scheme so `xcodebuild test -scheme Track` is reproducible.
- Verification: unit tests (storage round-trip = persist + survive-relaunch) + a UI test (launch-empty → add → relaunch-survives) + Simulator screenshot.

## Entry template

```markdown
### TRACK-NNN — <title>
**Source:** BACKLOG / user request
**Branch:** track/track-NNN-<slug>
**Acceptance criteria:**
- [ ] criterion (how it will be verified)
**Notes:** scope cuts, links, decisions while planning.
```
