# Current task

> One task at a time. When this file is empty, pull the next item from `BACKLOG.md`.

## Active

_(none active. **TRACK-000 complete** (✓ PR #161): Swift/iOS toolchain bootstrapped — Xcode 15.3 +
iOS 17.4 SDK/runtime; the `Track` Xcode project builds + runs in the Simulator (screenshot in
`reference/design/`); ADR-0001, `reference/swift-orientation.md`, and a verified
`reference/local-ci.md` landed. **Next up: TRACK-001 (WI-1 skeleton)** — `NavigationStack` + empty
Races-list root + persistence root dir + **pin the iOS deployment target** (per ADR-0001; the wizard
left it at 17.4). Copy its AC from the BACKLOG line into the template below and branch
`track/track-001-…`.)_

## Entry template

```markdown
### TRACK-NNN — <title>
**Source:** BACKLOG / user request
**Branch:** track/track-NNN-<slug>
**Acceptance criteria:**
- [ ] criterion (how it will be verified)
**Notes:** scope cuts, links, decisions while planning.
```
