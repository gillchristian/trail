# Done

Tasks that passed all verification gates. Newest at top.

Each entry: the TASK-NNN id, title, the date completed, a summary of what shipped (a sentence for trivial tasks, a paragraph when the detail earns its keep — the journal entry holds the full story), the PR number + merge sha, and a pointer to the journal entry.

```
- TASK-NNN — title — YYYY-MM-DD — summary — PR #N, merged `sha`. See journal YYYY-MM-DD [HH:MM]
```

## Completed

- TASK-047 — WI-1: `.trail` v2 identity + integrity guard — 2026-06-15 — Foundation of the coach-collaboration arc (ADR-0009): an imported `.trail` can be matched back to its race and refused when built on a different course. `Race` gains **`shareId`** — a stable cross-round-trip identity distinct from the IDB-key `id` (which is regenerated on import so a file can be imported twice): minted JS-side (`main.js`, `shareId || crypto.randomUUID()`) but **preserved on import**, so a coach's returned file links back — and **`courseHash`**, a fingerprint of the canonical decoded track (lat/lon→5 dp ≈1 m, ele→nearest m) via a new pure-Elm double-polynomial hash in `TrailSync` (no crypto, no port; tolerant of cosmetic GPX diffs — the user's Q1 choice of canonical-over-raw). New pure guard `TrailSync.classify incoming target → Mergeable | DifferentRace | DifferentCourse` (+ `verdictMessage`) hard-blocks a course mismatch (Q1). `.trail` → **v2** (`ProjectFile.currentVersion = 2`, gate widened to `{1,2}`); v1 files decode with both fields `""` and are stamped on import (shareId minted, courseHash computed from the embedded GPX); a v1 doc re-exports as v2. Codecs carry both fields with `D.oneOf` back-compat defaults. **Scope:** data + pure guard only — the update-from-file/merge UI that calls `classify`→merge is WI-3 (TASK-050); the existing import-as-new path is unchanged beyond stamping. Q1 resolved with the user; ADR-0010. Verified: all 7 gates green — type-check `Success!`, build `✓ built`, storage `SMOKE PASSED` (+ shareId minted/preserved/round-trips), aidcsv/sections/calibration `PASS`, and new **`smoke:trailsync`** `PASS` (hash determinism + cosmetic tolerance + distinctness; classify verdicts incl. empty-shareId-never-matches; v1/v2 decode, v3 rejected, v1→v2 re-export). Full browser upload→export→re-import→guard flow not driven headlessly (standing project limit) — recommended manual check. PR #91, merged `409eeee`. See journal 2026-06-15 23:18.
- TASK-046 — Brief nuance: file-based collaboration is in scope (Layer 0) — 2026-06-15 — First task of the coach-collaboration epic (ADR-0009). `project-brief.md`'s *Out of scope* flatly said "No social / sharing features" and "no multi-user", which the adopted plan now contradicts — the same drift the "No backend, ever" line had pre-Strava. Qualified "no multi-user" → "no *server-side* multi-user", and rewrote the social line to "No *server-side* social / sharing features — no accounts, no hosted documents, no real-time co-editing", with a parenthetical that **async, file-based, single-document collaboration** (export a `.trail` → coach annotates → merge back via three-way merge) is in scope as a Layer-0 feature riding the existing `.trail` round-trip, pointing at ADR-0009 / `coach-collab-spec.md`. Lines nuanced, not deleted (mirrors the Strava-softening pattern). Docs-only, `src/` untouched. PR #89, merged `4896f60`. See journal 2026-06-15 20:37 (intake) + 20:52 (close).
- TASK-045 — Section plan time is clock time (moving + aid rest); section Δ-vs-plan is clock-vs-clock — 2026-06-15 — The follow-up TASK-025 deferred ("section-table totals and section-card 'Time' stat untouched — would need the section-overlap fix first"), unblocked by TASK-039/ADR-0004. The section `Δ vs plan` compared planned **moving** seconds against actual **clock** seconds (`sectionActualSeconds` — real splits include the time spent at an aid), so resting at an aid but running exactly on pace read as ~that-much "behind plan." Now a section's **Time = clock** = `sectionMoving + sectionAidRest`, **Pace stays moving**, **Δ = actual − clock** — applied to the section table (`sectionsWithCumulative`: Time/Cum clock; aid rows became non-additive dividers showing rest as context; a footer caption), the section card (`viewSectionDetails`: Time stat clock + amber caption), and section-mode CSV (`buildSectionRows`: `section_time`/`cumulative_after_aid` clock, matching the UI + km-mode). New pure `Planning.sectionAidRest aids section` = rest of aids whose containing km (`kmAtDistance`) is in `section.kmIndices` — **by the aid's km, not `followedByAid`**: only that matches how the actual splits charge a stoppage (to the km that physically holds the aid, which the midpoint partition puts in exactly one section), so it's consistent even when an aid sits in the first half of its km (its rest then lands in the *next* section). `smoke:sections` extended (the harness previously hard-coded rest = 0) with scenarios E/F: `aidRest == [300, 0, 600]` (the first-half case → next section; `followedByAid` would wrongly give `[300, 600, 0]`), `Σ aidRest == 900 == Σ rests` (conservation), no-rest default `[0, 0]`. All six gates green. ADR-0008. The one manual check the headless env can't do — section table/card with a linked actual — is recommended post-merge. PR #86, merged `08c9a66`. See journal 2026-06-15 14:40.
- TASK-044 — Calibrate flat-trail pace from runnable kms — 2026-06-15 — Second calibration fit; sibling of TASK-043. New `Calibration.fitFlatPace`: over runnable kms (the predictor's own band, `abs slope < 0.04`) with a positive distance + recorded time, `pace = Σ distance / Σ time` (distance-weighted realized pace, s/km); returns value + counts or `Nothing`. Same realized-rate method as vmh (ADR-0007). The `#/profile` calibrate panel now shows both fits as rows (shared `calibRow`), each with its own Apply (`CalibrateFlatPace` sets + persists `flatTrailPaceSecPerKm`); the contributors line covers runs feeding either fit. `smoke:calibration` extended to 27 checks over both compiled fits (flat: known-input paces, the strict `<0.04` band cut, zero-time/zero-distance skip, no-data null, a both-fits-coexist case). All gates green; UI click identical to the vmh Apply the user already confirmed on real data. Completes the two core predictor rates (climb + flat) as data-driven fits. PR #82, merged `a76db2e`. See journal 2026-06-15 14:18.
- TASK-043 — Calibrate climb rate (`vmh`) from linked runs — 2026-06-15 — Batch task 5 of 5; first slice of the TASK-022 calibration epic. New pure `Calibration.fitVmh` computes the **gain-weighted realized climb rate** over climb kms (course gain ≥ 30 m with a positive recorded time) across the user's linked actual runs: `vmh = Σ gain / (Σ seconds / 3600)`; returns the value + contributing climb-km/run counts, or `Nothing` for no climb data. Uses only data already held (course gain via `kmsCache` + `actualSplits.splits`) — no new Strava fetching. Surfaced on `#/profile` as a transparent, opt-in "Calibrate from your runs" panel (fitted rate + contributing run names + current value → explicit Apply, which sets + persists `verticalRateVmh`); degrades to a hint when no run is linked. ADR-0006 (methodology: gain-weighted, 30 m threshold, transparent; alternatives deferred). Verified by a new `smoke:calibration` harness (17 checks over the real compiled fit: gain-weighting, threshold cut, no/zero-time skip, no-data null, multi-run); all gates green; UI click-path flagged for a manual check. PR #80, merged `819e9dc`. See journal 2026-06-15 13:42.
- TASK-042 — Print-friendly planning table — 2026-06-15 — Batch task 4 of 5. Added a **Print** button on the plan page (new `Dom.print` port → `window.print()`) and an `@media print` stylesheet (`app.css`). App chrome (header, footer, breadcrumb, target panel, predictor slider, actual-run strip, tabs/buttons) carries Tailwind's `print:hidden`; the race header + table are wrapped in `.plan-print` and restyled black-on-white with cell borders, a repeated header row (`thead{display:table-header-group}`), `break-inside:avoid` rows, and a compact 11px table font. One print path covers both km and section modes; no new route; on-screen rendering unchanged (print-scoped rules only). Verified: type-check/build/3 smokes green, `window.print` port + `.plan-print`/`table-header-group`/`break-inside` rules confirmed in the built bundle/CSS. The actual print-preview legibility is the one thing the headless env can't check — flagged for a manual ⌘P review. PR #78, merged `c2d30b4`. See journal 2026-06-15 13:31.
- TASK-041 — Correct the `slopeFactor` docstring (un-normalized values) — 2026-06-15 — Batch task 3 of 5; closes the follow-up TASK-038 queued. The `Planning.slopeFactor` docstring still quoted the un-normalized factors ("10 % uphill ≈ 1.69×; 20 % grade either way ≈ 2.40×"). Replaced with the normalized, recomputed values matching ADR-0003: symmetric about s = −0.05, f(+0.10)≈1.42, f(−0.10)=1.0, f(+0.20)≈2.01, f(−0.20)≈1.42; kept the correct f(0)=1.0 / s=−0.05-minimum framing. Grep confirmed these were the only stale `1.69`/`2.40`/"either way" copies in `src/`. Comment-only, no behavior change; all five gates green. PR #76, merged `c580bdf`. See journal 2026-06-15 13:19.
- TASK-040 — Separate `gpxText` into its own IDB row — 2026-06-15 — Batch task 2 of 5. `Race.gpxText` (~3 MB UTMB-size) was inline in the single `races` store, so all 14 `saveRace` sites — almost all plan/aid/meta/actual *edits*, incl. the slider-commit hot path — re-shipped it across the port + rewrote it to IDB though it never changes after import. Added a `gpx` object store (`{id, gpxText}`, DB v2→v3 with an `onupgradeneeded` migration that splits existing inline GPX). New light save path: `Storage.saveRaceMeta`/`Types.encodeRaceMeta` (shared field list with `encodeRace` so they can't drift) writes only the `races` row from a `gpxText`-free payload — the 12 edit sites use it; the 2 import sites keep the full `saveRace` (writes both stores, echoes the full new race). `loadAllRaces` re-joins `gpxText`; decoder defaults it to `""`; `RaceSaved` refills it from the in-model race on a light echo; `deleteRace` clears both rows (no orphan). `race.gpxText` stays in the Elm model; `.trail` export unchanged (no format bump). ADR-0005. Verified: rewrote `smoke-storage.mjs` to a faithful v3 mirror — 24 checks incl. the split, light-save-leaves-gpx-untouched, orphan-free delete, and the v2→v3 migration of a 2.29 MB GPX (`SMOKE PASSED`); type-check/build/aidcsv/sections green; compiled bundle emits the `storageSaveMeta` port; dev server boots. Manual browser round-trip not run (env can't drive the compiled Elm bundle — project's standing storage-verification limitation); recommended as a final manual check. PR #74, merged `a922894`. See journal 2026-06-15 13:14.
- TASK-039 — Section-overlap double-count fix in `Planning.sectionsForRace` — 2026-06-15 — First of the five-task batch the user promoted on 2026-06-15. A 1 km window straddling an aid-station distance matched the overlap test (`distStart < b && distEnd > a`) for *both* adjacent sections, so its index landed in two `kmIndices` lists and `sumKmField` summed its gain/loss twice — double-counting section-table Time/gain/loss/cum, the section card, the actual-vs-plan Δ (`sectionActualSeconds`), and section-mode CSV. Now each km joins the single section whose half-open `[a, b)` interval contains its midpoint — a clean partition (no km in two sections, none dropped); also dropped `sumKmField`'s unused `a`/`b` args and fixed the misleading comments. New `npm run smoke:sections` gate (`src/SectionsHarness.elm` + `scripts/smoke-sections.mjs`) drives the real compiled function and asserts partition + gain/loss conservation across straddle/on-boundary/no-aid/gain+loss scenarios; confirmed it *fails* on the pre-fix overlap test (scenario A → `[[0,1],[1,2,3],[3,4]]`, Σ gain 700 not 500). ADR-0004 records the midpoint-vs-pro-rate decision; `local-ci.md` + the manifest gained the 5th gate. PR #72, merged `633e263`. See journal 2026-06-15 12:57.
- TASK-038 — ADR + local-CI + MORNING technical-accuracy fixes — 2026-06-10 — Third and last of the 2026-06-10 doc-vs-code audit fixes; cleared the BACKLOG Active queue. ADR-0003: slope-factor properties table corrected from un-normalized to the normalized `slopeFactor` values (f(+0.10)=1.419 not 1.687, f(−0.10)=1.000) and noted the curve is symmetric about s=−0.05 not 0; dropped the nonexistent "Reset plan" interaction (only per-km `ResetKmToAuto`) and the wrong all-Manual "derived target" (committed target is kept, `effectiveTargetSeconds`); noted the slope divisor is the window length not a fixed 1000 m + the auto-km rounding drift. ADR-0002: `sym` auto-derived from services (`symbolForAid`, no UI picker, no-services default "Flag, Blue" not Restaurant), `<desc>` example fixed to `buildDesc`'s real format, removed the nonexistent "Aid N" name fallback. cadence-spec + addendum: streams example now shows the `{"data":[…]}` per-key nesting `StravaStreams.parse` decodes; env var `VITE_BACKEND_URL`; "fields flow through to settings" reframed (no `/api/athlete` client yet); "§14.2" citation repointed to the archived spec. local-ci.md: documented the global Elm 0.19.1 prerequisite + Node v22 pin, qualified the storage-smoke scope. MORNING.md: frozen-2026-05-15 historical banner, dev port 5174, parking lot mid-BACKLOG, refreshed the stale nvm-22/storage-smoke caveat. Docs-only, `src/` untouched; all claims code-verified. PR #69, merged `9529c01`. See journal 2026-06-10 08:52.
- TASK-037 — Brief + glossary rewrite to match shipped reality — 2026-06-10 — Second 2026-06-10 doc-vs-code audit fix. `project-brief.md`: the load-bearing "No backend, ever" / "No backend / multi-user / sync" constraints reworded to the roadmap's agreed hybrid (Layer 0 offline / Layer 1 opt-in Strava sync via the shared `cadence` backend, which stores no trail state); new "Features shipped beyond the original list" section (plan-vs-actual + HR via `ActualGpx`/`StravaStreams`; athlete profile + `Predictor` at `#/profile`; aid CSV via `AidCsv`); feature 10 corrected (map is the `<trail-map>` custom element, not a JS port); drifts fixed (km-only is a UI rule, CSV import accepts `distance_mi`; `Browser.application` not `.element`; IDB port ~100 lines / 2 stores not ~50 / 1). `glossary.md`: **VMH** redefined from the wrong "flat-ground km/h" to its real meaning — vertical climb rate (m ascent/hour, `verticalRateVmh`, `climb = gain/(VMH×i)`, verified `AthleteProfile.elm:47` + `Predictor.elm:114`); added **flat trail pace** as the distinct flat rate, plus distance category (S/M/L/XL), elevation density, flat-equivalent distance. All claims code-verified. PR #67, merged `73b206f`. See journal 2026-06-10 08:44.
- TASK-036 — Ship-status sync across planning + reference docs — 2026-06-10 — First of the three 2026-06-10 doc-vs-code audit fixes. The predictor + athlete-profile + Strava-sync arc (TASK-014..021, 024/024b) had shipped but several docs still framed it as a pending proposal. Flipped `pace-prediction-roadmap.md` (status header → "largely implemented"; §0/§2/§12 drop proposed/distributor-only framing for the shipped `Predictor.predict`; §8 line-296 brief-update promise now notes TASK-024 shipped + tracked by TASK-037), `cadence-backend-spec.md` (header → "implemented", §9 trail-side list marked built), addendum-1 (the two spots treating trail's TASK-024 as unshipped; addendum's own cadence-side scope stays pending), BACKLOG Proposals (TASK-014..021 struck through + PR-annotated, only TASK-022 left open), and `whiteboard/training-as-analysis.md` (TASK-026 no longer "Queued"). Code-verified (`Predictor.predict` at `src/Predictor.elm:53`, `StravaApi.elm` consumes live cadence endpoints). Out of scope: brief/glossary rewrite (TASK-037), ADR/CI/MORNING accuracy (TASK-038), roadmap appendix formulas (audit confirmed they match code). PR #65, merged `7b8455f`. See journal 2026-06-10 08:36.
- TASK-035 — Labyrinth principle: record the maze, not just the exit — 2026-06-09 — New principle in `framework/principles.md` naming the value behind the journal's failed-attempts rule, the ADR's "alternatives considered", and the whiteboard's purpose: dead ends + reasoning at forks are part of every artifact's deliverable (prescription = what, description = why). Cited to Alvaro Videla's "Notes on the synthesis of labyrinths" (copy at `reference/labyrinth.md`; framework cites by author/title to stay self-contained). `working-style.md` gains a "Paths not taken" bullet with curation guidance (amplify lessons, abbreviate noise); cross-links from the manifest layout + whiteboard README. Framework v1 → v2, first exercise of the upstream-change convention. PR #61, merged `af65751`. See journal 2026-06-09 18:18.
- TASK-034 — Framework/project split + extractable framework with pluggable delivery — 2026-06-09 — `knowledge/framework/` now holds the complete project-agnostic working system (7 flat files; philosophy docs moved with `git mv`): `README.md` (abstract loop, v1 + upstream stamp), `SETUP.md` (adoption guide, installed-guard, inline skeletons — no templates tree), `principles.md`/`verification.md`/`when-stuck.md`/`working-style.md` (de-gitted), and `delivery.md` — the pluggable piece: profiles `pr`/`commits`/`none`, shared commit conventions, point-of-use attribution hook, exact VCS command policy under `none`, per-task override contract (recorded in CURRENT.md, task-scoped, push/force/PRs never implied, bounded by manifest hard constraints), undeclared mode = `none` fail-safe. `knowledge/README.md` rewrote into the project manifest (greppable `delivery: pr`, project rules, instantiated loop, instance-free guard grep); `CLAUDE.md` carries the explicit reading chain; tombstone at `philosophy/README.md`; live refs fixed (local-ci, roadmap). Design adversarially panel-reviewed (3 lenses) before building; verified by two fresh-agent dry-runs (trail chain probe + literal SETUP.md adoption as a delivery-none corporate project), both "holds-up"; their findings fixed pre-merge. PR #59, merged `bc0214f`. See journal 2026-06-09 17:58.
- TASK-033 — Knowledge-base tidy (review findings) — 2026-06-09 — Systematic review of `knowledge/` (cross-references, consistency, staleness, soundness; every finding adversarially verified), then the fixes: new `reference/local-ci.md` recording the real gate commands (all four run green first; the phantom "lint" step excised from CI phrasing in `pr-workflow.md`/`verification.md`/`README.md`), squash-only merging made consistent with the brief's hard constraint (`--merge` escape dropped), `docs/task-NNN-close` bookkeeping-PR convention codified, DONE/journal placeholders backfilled (`8449767`, PR #18 `b451e1e`) + duplicate TASK-008/009/010 stub removed, CURRENT.md entry template added, BACKLOG conventions (checked-in-place index, global TASK-id allocation) documented, glossary populated after 32 tasks empty, CLAUDE.md quick map gains `whiteboard/`, stale CSV-whiteboard follow-ups rewritten past-tense, ADRs "permanent record" not "immutable". Refuted findings deliberately not acted on (checked-in-place BACKLOG, point-in-time whiteboard entries, per-rule screenshot deletions). PR #57, merged `de1b946`. See journal 2026-06-09 17:33.
- TASK-032 — "Crew access" aid-station service — 2026-06-09 — 7th `Service` variant `Crew` (label "Crew access", 🤝, key `crew`): `Types.elm` service functions updated (compiler-enforced), `AidCsv.serviceFromToken` accepts crew/assistance token family (`crew`, `crew access`, `assistance permitted`, `support crew`, …), `leaflet-element.js` `SERVICE_EMOJI`/`SERVICE_LABEL` gain `crew`. Settable via manual form chips (free, via `allServices`) and CSV; renders wherever services render. Smoke gained section J (crew tokens, case-insensitivity, combined `water|crew`). PR #55, merged `9f4a54f`. See journal 2026-06-09.
- TASK-031 — Aid-station CSV import/export (+ cutoff, warm food, notes, scroll-to-editor) — 2026-06-05 — New pure `AidCsv.elm` (`parse`/`toCsv`): lenient organiser-table import (header-or-positional, `distance_km`/`distance_mi` unit by column name → metric, RFC-4180 tokenizer handling quotes/CRLF/BOM/`;`/decimal-comma, partial import with per-row errors + warnings, `rest` defaulting from the active profile's `aidStyleSecondsPerStation`). Race page gains **Import CSV** (file → preview of parsed stations + skipped rows + warnings → replace-with-confirm; fresh ids continued from `aidStationSeq`; plan is km-indexed so replace never orphans it) and **Export CSV** (re-importable). `AidStation` gains `cutoff : Maybe Int` (elapsed seconds from start; back-compat `Nothing` decoder, no `.trail` version bump) settable via CSV + manual form, shown on rows. New 6th `Service` variant **Warm food** (🍲, key `warm_food`; CSV tokens warm/hot food/soup/broth). Full **notes** support: editable textarea in the aid form (`validateAidForm` keeps notes instead of `""`) + displayed in the aid list, import preview, km table, section table, and per-km / per-section planning pages. New minimal `Dom.scrollIntoView` port smooth-scrolls the editor into view on Add/Edit (deferred one rAF so Elm renders first). Verified by `scripts/smoke-aid-csv.mjs` driving the real compiled parser via a `Platform.worker` harness (`src/AidCsvHarness.elm`, never bundled). PR #53, merged `422d118`. See journal 2026-06-05.
- chore — samples cleanup + archive spec + perf trace tool — 2026-05-18 — Aggressive cleanup of `samples/` per user pick: removed 15 unused UI mockups (`f-*`, `profile-0N`, `profile-02-strava`, `race-cards`, `route-cards`, `profile-interrupted`). Kept aid-station.png, the three GPX files (perf fixtures), sample.gpx, coros_pace_strategy.html. Moved `trail_race_planner_spec.md` → `knowledge/reference/archive/`. Updated brief's visual-direction paragraph and pace-prediction-roadmap path. Added `scripts/profile-trace.mjs` + `npm run perf:trace` for ad-hoc profile-pipeline perf testing across GPX sizes. PR #43, merged `8449767`. See journal 2026-05-18.
- TASK-030 — Predictor-default target on plan view when no target saved — 2026-05-18 — New `effectiveTargetSeconds : Profile -> Race -> List Km -> Int` falls back to `Predictor.predict profile race kms 1.0 |> .totalS` when `race.plan.targetSeconds = Nothing`. Plumbed through all five `Planning.distribute` call sites (viewPlanTable, viewPlanSection, viewPlanKm, ExportCsvKms, ExportCsvSections). Plan table now populates immediately on first navigation; slider commits a real value on first interaction. PR #42, merged `3d296e5`. See journal 2026-05-18.
- TASK-029 — Chunked SVG profile path for long tracks — 2026-05-18 — Cocodona 250 (~400 km, 36 746 trkpts) at 10 m/px produces a ~39 000 px wide SVG. Browser SVG path renderers truncate single huge `<path>` elements at a soft per-element limit (~16-20 k px in Chromium). Headless-Elm trace confirmed `Gpx.parseGPX` + `Gpx.simplify` produce the full coord list correctly — issue is browser-only. Fix: new `chunkByXExtent : Float -> List (Float, Float) -> List (List (Float, Float))` in `Profile.elm` splits coords into ≤10 000 px-wide chunks, emits one `<path>` per chunk for area / stroke / ghost layers. Adjacent chunks share their boundary point so the rendered line stays continuous. UTMB sits within a single chunk (no-op for tracks under the limit). PR #41, merged `3612aeb`. See journal 2026-05-18.
- TASK-028 — Home page split into Plans and Executions — 2026-05-18 — New `viewRaceSections` partitions the index by `actualSplits` presence. Plans (no actual) sorted by `race.date` asc with undated last by createdAt desc. Executions (linked actual) sorted by `actualSplits.uploadedAt` desc. Each non-empty section gets a heading + count + sub caption + the existing `viewRaceGrid`. Empty sections hidden. No per-card styling change in this PR. PR #37, merged `4b8b2ae`. See journal 2026-05-18.
- TASK-027 — Pulse + skeleton on home drop area while parsing — 2026-05-18 — New `StartParse fileName content` Msg moves the synchronous parse branch (`isProjectFile` → `ProjectFile.decode` / `Gpx.parseGPX`) out of `GotContent`. `GotContent` only sets `upload = Parsing fileName` and dispatches `StartParse` via `Task.perform (\_ -> ...) (Process.sleep 1)`, giving the renderer a tick to paint before the parse blocks. `viewUploadBanner` splits into `viewUploadIdle` / `viewUploadSkeleton`; the skeleton variant adds `animate-pulse` plus three slate-700 skeleton bars and drops the clickable button. Status copy tightened ("Processing X… / Crunching the track…"). PR #36, merged `bd6038b`. See journal 2026-05-18.
- TASK-026 — Avg HR per km on linked actuals — 2026-05-18 — `ActualPoint` gains `hr : Maybe Int`. `StravaStreams.parse` decodes the `heartrate` stream (cadence already requests it) and populates per-point HR; `buildPoints` rewritten as a tail-recursive 4-stream walker (sidesteps Elm's 4-tuple ban + the cons-tail trap). New `ActualGpx.computeHrPerKm` averages per km. `Types.ActualSplits` gains `hrPerKm : Maybe (Dict Int Int)` with back-compat decoder (D.oneOf … D.succeed Nothing). Per-km card grid flips 2-col → 3-col with an Avg HR cell ('—' for kms with no samples). km table adds an "Avg HR" column when `hrPerKm` is `Just`. PR #35, merged `68cb869`. See journal 2026-05-18.
- TASK-025 — Pace bug fix: per-km Target = clock time, Pace = moving — 2026-05-18 — New `aidRestInKm` helper (uses `Planning.kmAtDistance` so each aid belongs to exactly one km). Per-km card, km table, per-section card's contained-km row, and CSV km-mode now display Target as clock time (`result.seconds + stopRest`). Pace stays moving-only. Δ vs plan now compares clock-clock. `CommitKmTimeForKm` parses input as clock and stores `Manual (max 0 (typed − kmRest))` so distribution math stays moving. Amber caption added when stopRestInKm > 0. Section-table totals and section-card "Time" stat untouched (would need pre-existing section-overlap fix first; logged in parking lot). PR #34, merged `0f316fb`. See journal 2026-05-18.
- chore — knowledge/whiteboard/ area + queue 4 follow-up tasks — 2026-05-18 — New `whiteboard/` directory with README + two discussion entries (profile-management, training-as-analysis). README + key-docs index updated. BACKLOG gains TASK-025..028. PR #33, merged `49feb1c`. See journal 2026-05-18.
- TASK-024b — Strava activity picker + streams fetch + persist — 2026-05-15 — New `StravaApi.elm` (HTTP wrapper for cadence's `/api/activities` + `/api/activities/{id}/streams`, with `Authorization: Bearer` + JSON decoders). `Model.stravaPicker : StravaPicker` state machine (Closed / LoadingActivities / Showing / LoadingStreams / Error). Plan-table actual-run strip gains an orange "Link from Strava" button when the user is connected and the race isn't linked yet. Click → fetch /api/activities → modal lists 60 days of activities (name + date + distance + moving time + sport type) → click an activity → fetch streams → parse via `StravaStreams.parse` → compute splits via existing `ActualGpx.computeSplits` → save on the race. Error handling for 401 (with "reconnect Strava" hint), timeout, network failure, parse failure. `elm/http` added. PR #26. See journal 2026-05-15 22:55.
- TASK-024 (v1) — Strava OAuth round-trip + Connect/Disconnect — 2026-05-15 — JS-side captures `?token=...` from the OAuth callback URL before Elm boots, sends it to Elm via flags. Three new IDB-port pairs (`storageLoadStravaToken` / `storageSaveStravaToken` / `storageStravaTokenLoaded`) persist the token under `settings.stravaSessionToken`. `Model.stravaToken : Maybe String` + `Model.backendUrl : String` (from flags, defaults `http://localhost:3001`, overridable via `VITE_BACKEND_URL`). Settings page gains a Strava section: orange "Connect Strava" link → `${backendUrl}/auth/strava?origin=trail` when not connected; emerald "Connected" pill + Disconnect button when connected. **Activity picker / streams fetch / link-to-race deferred to TASK-024b** — keeps this PR small enough to verify. PR #25. See journal 2026-05-15 22:35.
- TASK-021 — Strava streams parser — 2026-05-15 — New `StravaStreams.elm` decoder for the keyed-object shape cadence's `/api/activities/{id}/streams` returns. Converts `{time, distance, latlng, altitude}` streams into the same `ActualGpx.ActualTrack` value that `ActualGpx.parse` produces, so the existing `computeSplits` + diff column logic stays source-agnostic. Pure module; TASK-024 will wire it into the upload flow. `ActualGpx.elm` exposes `cumulativeDistances` too (not yet used here — we use Strava's own cumulative distance directly — but worth exposing now). PR #24. See journal 2026-05-15 22:15.
- TASK-020 — Confidence indicator on predictor output — 2026-05-15 — New `confidenceFromProfile : Model -> Race -> (label, tone, margin)` returns `("Low · profile from presets", slate, 0.20)` by default and `("Medium-low · 1 actual linked", sky, 0.15)` when the race has `actualSplits`. Predictor strip displays `± hh:mm` based on the margin (e.g. ± 0:42 on a 3:30 prediction) plus the band label. Honest about the precision until TASK-022 calibration narrows it further. PR #23. See journal 2026-05-15 22:00.
- TASK-019 — Bidirectional aggressiveness slider — 2026-05-15 — New `viewPredictorStrip` between the existing target panel and the actual-run strip on the plan-table page. Native `<input type="range">` from 0.80 to 1.25, step 0.01. Slider position derived from `Predictor.solveForIntensity(targetSeconds)`; dragging triggers `SliderChanged → Predictor.predict(intensity).totalS → save plan.targetSeconds`. Shows current effort band ("Conservative" / "Goal" / "Push" / "All-in" with rose/amber/sky/emerald accents, plus "Below conservative" and "Beyond all-in" out-of-bracket warnings), predicted finish + component breakdown (climb / descent / runnable / aid seconds), and the active profile's VMH + pace as a brief label. Hidden when kms haven't parsed yet. PR #22. See journal 2026-05-15 21:50.
- TASK-018 — Predictor.predict — Layer B time model — 2026-05-15 — New `Predictor.elm` exposes `Prediction` record + `predict : Profile -> Race -> List Km -> Float -> Prediction` and `solveForIntensity` (12-iteration bisection on [0.80, 1.25]). Per-km decomposition: slope ≥ +4 % → climb (`gain / (vmh × i)`); slope ≤ −4 % → descent (`flatPace × Tobler × descentSkillMult / i`); else runnable (`flatPace × Tobler × techMult / i`). Aid time from `aidStyle × aid_count`. Single-pass fatigue multiplier on the moving sum. Pure module — no caller yet; TASK-019 wires the slider. PR #21. See journal 2026-05-15 21:30.
- TASK-017 — Athlete profile data model + IDB + settings page — 2026-05-15 — New `AthleteProfile.elm` (~360 lines) with `Profile` record, four presets (Beginner / Mid-pack / Strong mid-pack / Sub-elite), `DescentSkill`/`TechSkill`/`AidStyle` enums + skill multipliers, JSON codecs using stable key strings. JS-side IDB bumped to version 2, new `settings` object store (key-value, single row `activeProfile`). Three new Elm ports (`storageLoadProfile`, `storageSaveProfile`, `storageProfileLoaded`). New `#/profile` route + "Profile" link in the header. Settings page: preset row + 9 editable fields (VMH, pace, fatigue threshold/slope, dropdowns for skills, optional HR fields) + Save button with "Saved ✓" feedback. Profile loads on app boot; falls back to `midPack` default if no IDB row exists. PR #20. See journal 2026-05-15 21:00.
- TASK-016 — Planned-vs-actual upload (manual GPX) — 2026-05-15 — New `ActualGpx.elm` (~280 lines): GPX-with-time parser via regex, inline ISO 8601→seconds (Hinnant "days from civil"), Haversine cumulative distance, interpolated per-km split computation. `Race.actualSplits : Maybe ActualSplits` added with `{splits, totalSeconds, totalDistance, uploadedAt}`; back-compat decoder defaults to `Nothing`. Plan-table page gained a "Link actual run" strip — file picker when not linked; summary (total time, distance, vs-target delta) + Replace/Unlink when linked. km-table grows two extra columns (Actual, Δ vs plan) when splits exist, rose-when-slower / emerald-when-faster. `elm/time` promoted from indirect to direct. PR #19. See journal 2026-05-15 19:30.
- TASK-015 — Per-km grade classification chip — 2026-05-15 — New helper `gradeClass : slope -> (label, tone-class)` in `Main.elm` with 5 buckets per spec §3.2 (Steep climb / Climb / Runnable / Descent / Steep descent). Planning km-table gained a "Grade" column between Δ ele and Pace, rendering a compact pill with ring-inset outline and rose→slate→emerald tone palette matching the existing Δ ele cell. Section table untouched (mixed grades). PR #18, merged `b451e1e`. See journal 2026-05-15 17:40.
- TASK-014 — Course summary card additions — 2026-05-15 — Three pure helpers in `Main.elm` (`elevationDensity`, `densityLabel`, `equivalentFlatKm`). Race card category-label line gained a colour-coded density tag (e.g. `SHORT · MOUNTAINOUS · 41 m/km`, slate → amber → rose by bucket). Race detail header grid grew from 3 to 5 stats (Distance / Gain / Loss / Density / Flat eq.), responsive `grid-cols-3 lg:grid-cols-5`. First concrete slice from the pace-prediction roadmap (§1.A + §11.A). PR #16. See journal 2026-05-15 15:50.
- TASK-013 — Real-world map view (Leaflet + OSM) — 2026-05-15 — Custom element `<trail-map>` wraps Leaflet. Accepts `track` (JSON-encoded `[[lat,lon],…]`) and `markers` (JSON-encoded `[{lat,lon,index,name}]`) as attributes. Polyline rendered as a thick translucent rose halo + a thin rose-300 core; numbered amber divIcons for aid stations. Service worker gets a tile-specific code path: cache-first for `*.tile.openstreetmap.org/Z/X/Y.png`, FIFO eviction at 800 tiles (~25 MB). Tiles you've panned across are then available offline. PR #10, merged as `0967010`. See journal 2026-05-15 13:30.
- TASK-003 — Race metadata editing + cover image — 2026-05-15 — "Edit details" button on race detail opens an inline form for name / date / location / URL / notes / cover image. Cover image picked via a new `Download.pickImageFile` port (JS-side `FileReader.readAsDataURL` so we store a data URL, not a `blob:` URL that wouldn't survive reload). Race detail renders a hero banner when a cover is set; race cards on the index use the cover as a background strip with a dark gradient overlay. PR #9, merged as `bf45fa1`. See journal 2026-05-15 12:00.
- TASK-011 — Gamified visual pass — 2026-05-15 — UTMB-DNA aid-station badges (circular ring + number + name pill), ghost-wave layered elevation rendering (8 echo strokes with fading opacity for the signature sound-wave depth), rose→amber gradient stroke on profile, race cards with category accent stripe + S/M/L/XL bucket badge + hover lift/glow, gradient wordmark + inline mountain logo, stroke-draw + card-fade-in animations with `prefers-reduced-motion` opt-out. PR #8, merged as `3f410dc`. See journal 2026-05-15 11:15.
- TASK-012 — Offline-first / PWA — 2026-05-15 — Hand-rolled service worker (≈70 lines) registered only in production builds (`import.meta.env.PROD`). App-shell pre-cache on install; stale-while-revalidate for same-origin GETs; old caches cleared on activate. PWA manifest with SVG icons, dark theme, standalone display, apple-touch / iOS meta tags. App works offline after first load (data already lived in IDB). PR #7, merged as `6766b5e`. See journal 2026-05-15 10:00.
- TASK-008 + TASK-009 + TASK-010 — file exports (CSV / GPX / .trail) + .trail import — 2026-05-15 — `Download.elm` port + Blob/`<a download>` JS wiring. `Csv.elm` builds km-mode and section-mode CSV with RFC-4180 quoting. `GpxExport.elm` re-emits the original GPX with `<wpt lat/lon/ele/name/desc/sym/type>` waypoints inserted before `<trk>` per ADR-0002. `ProjectFile.elm` round-trips a `Race` as a `{format, version, race}` envelope (`.trail`). Index upload picker accepts both `.gpx` and `.trail`; on import we drop the id so the JS side assigns a fresh one. Race detail gains an Export panel (GPX + .trail); plan table gains a "Download CSV" button that mirrors the active mode. PR #6, merged as `5b3b46d`. See journal 2026-05-15 09:15.
- TASK-006 + TASK-007 — per-km planning + Tobler GAP distribution — 2026-05-15 — `Planning.elm` (pure math: 1 km windowing with edge-interpolation, Tobler-normalised slope factor, distribution honouring `Manual` locks and aid rest, sections-between-aid). Two new routes (`/plan` table, `/plan/:km` per-km card). Race detail gets an "Open the plan →" CTA. Table view toggles km / section. Per-km card draws a 1:1 mini-profile (~360 px wide, iPhone-friendly) with stop markers; right-column form edits time (M:SS or "auto") and notes; "Reset to auto" reverts a manual lock. Target time committed on blur; per-km edits saved on blur. PR #5, merged as `1293fc8`. See journal 2026-05-15 07:30.
- TASK-005 — aid-station CRUD on the race detail page — 2026-05-15 — Aid stations live on the Race record (per-race id seq, no uuid lib). Inline add/edit form with "from start" / "from previous" toggle, rest-minutes, services multi-select (water/food/medical/wc/drop-bag). Markers render on the profile chart. Backwards-compatible decoder defaults to [] for old IDB records. PR #4, merged as `62b9d84`. See journal 2026-05-15 05:30.
- TASK-004 — true-1:1 elevation profile view — 2026-05-15 — Profile.elm lifted from crest with dark-theme retune (rose gradient, slate grid), in-model parsed-track cache so UTMB-size GPX parses once on RacesLoaded and renders instantly afterward, viewport-aware container width via Browser.Events.onResize. PR #3, merged as `1287f25`. See journal 2026-05-15 04:00.
- TASK-002 — race storage + index + routing — 2026-05-15 — Browser.application shell with hash router, IndexedDB-backed Race persistence, race index grid with delete-confirm, race detail stub. PR #2, merged as `34b8c7b`. See journal 2026-05-15 03:00.
- TASK-001 — scaffold Elm + Vite + Tailwind app — 2026-05-15 — Elm 0.19 + Tailwind v4 + Vite project bootstrapped, Gpx.elm lifted from crest, file-upload happy path renders track stats. PR #1, merged as `0419712`. See journal 2026-05-15 01:35.
